<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>{{ page.frontmatter.title }} | {{ site.title }}</title>
    <script>
        hljs.highlightAll();
    </script>
</head>

<div class="container">
    <nav class="nav">
        <a class="nav-item" href="{{ site.base_url }}">
            <h1 class="title is-4">{{ site.title }}</h1>
        </a>
    </nav>
</div>
